import { StyleSheet } from 'react-native';
import colors from '../../config/colors';

const ListItemSeparatorStyles = StyleSheet.create({
  itemSeparator: {
    width: "100%",
    height: 1,
    backgroundColor: colors.lightGrey,
  }
})

export default ListItemSeparatorStyles;